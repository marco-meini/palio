{
  "name": "palio",
  "private": true,
  "version": "1.1.2",
  "scripts": {
    "dev:api": "npm run dev:api --prefix server",
    "dev:client": "npm run start --prefix client",
    "dev:fe": "npm run dev:client",
    "dev": "concurrently -n api,client -c blue,green \"npm run dev:api\" \"npm run dev:client\"",
    "test:server": "npm test --prefix server",
    "test:be": "npm test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
